git repo -https://github.com/Adityakhot13/simple-website.git


Exp -4 Docker container 
dockerfile-

my-webapp/
│
├── Dockerfile
└── website/
      ├── index.html
      ├── styles.css
      └── script.js

#Dockerfile

# Use official nginx image
FROM nginx:latest

# Copy your static website into Nginx public directory
COPY website/ /usr/share/nginx/html/

# Expose port 80
EXPOSE 80



docker build -t my-nginx-webapp .

docker run -d --name webapp -p 8080:80 my-nginx-webapp


#index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Simple Login</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>

  <div class="container">
    <h2>Login</h2>

    <input type="text" id="username" placeholder="Username" />
    <input type="password" id="password" placeholder="Password" />

    <button onclick="login()">Login</button>

    <p id="status"></p>
  </div>

  <script src="script.js"></script>
</body>
</html>


#style.css

body {
  background: #f2f2f2;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  font-family: Arial;
}

.container {
  width: 300px;
  padding: 20px;
  background: white;
  border-radius: 10px;
  text-align: center;
  box-shadow: 0px 0px 10px rgba(0,0,0,0.2);
}

input {
  width: 90%;
  padding: 10px;
  margin: 10px 0;
}

button {
  width: 95%;
  padding: 10px;
  background: #4285f4;
  border: none;
  color: white;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background: #3273dc;
}

#status {
  margin-top: 10px;
  font-weight: bold;
}


#script.js

// Simple hard-coded credentials
const USER = {
  username: "admin",
  password: "1234"
};

function login() {
  const username = document.getElementById("username").value;
  const password = document.getElementById("password").value;
  const status = document.getElementById("status");

  if (username === USER.username && password === USER.password) {
    status.style.color = "green";
    status.innerText = "Login successful! ✔️";
  } else {
    status.style.color = "red";
    status.innerText = "Invalid username or password ❌";
  }
}



##exp - compose

# dockerfile- 

FROM php:8.2-apache

# Install mysqli extension
RUN docker-php-ext-install mysqli && docker-php-ext-enable mysqli

# Copy PHP file into container
COPY index.php /var/www/html/

#index.php

<?php
$servername = "db";
$username = "root";
$password = "rootpass";
$dbname = "testdb";
// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
// Check connection
if ($conn->connect_error) {
 die("Connection failed: " . $conn->connect_error);
}
echo " Connected successfully to MySQL!";
?>

#docker-compose.yml

version: '3.8'

services:
  web:
    build: ./web
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: testdb
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:

#Readmne



docker compose experiment
web-app
    docker_compose.yml
      |
      web- index.php
          -dockerfile


command :  docker-compose up --build
visit :    http://localhost:8080




##Exp - Jenkins

#Jenkinsfile
pipeline { 
    agent any 
 
    stages { 
        
 
        stage('Build') { 
            steps { 
                echo 'Building the app...' 
            } 
        } 
 
        stage('Test') { 
            steps { 
                echo 'Running tests...' 
            } 
        } 
 
        stage('Deploy') { 
            steps { 
                echo 'Deploying to server...' 
            } 
        } 
    } 
}


#Readme
https://github.com/Adityakhot13/simple-website.git

docker run -d --name jenkins-server -p 8081:8080  -p 50000:50000 -v jenkins_home:/var/jenkins_home jenkins/jenkins:lts





##Exp - Winston logging

#readme
✅ How to Run the Node.js + Express + Winston Logging Project
1. Install Node.js

If you don’t have Node installed:

Download it here:
👉 https://nodejs.org/

Verify installation:

node -v
npm -v

✅ 2. Extract the ZIP

Unzip the file:

simple_web_app/


Open a terminal/command prompt inside this folder.

✅ 3. Install Dependencies

Run this inside the project folder:

npm install


This installs:

Express

Winston

Morgan

✅ 4. Start the Server

Run:

node app.js


If you want auto-restart when files change:

npm install -g nodemon
nodemon app.js

✅ 5. Open the Web App in Browser

Go to:

👉 http://localhost:3000

✅ 6. View Logs

Logging files will appear automatically:

logs/app.log → Winston logs (info, errors)

console output → Morgan logs (HTTP requests)



#Fule structure 
  in main folder - app.js  ,logger.js ,package.json , and folder public
  in public folder - app.js , index.html , style.css


#app.js(for main)
const express = require('express');
const path = require('path');
const logger = require('./logger');
const morgan = require('morgan');

const app = express();
const PORT = 3000;

app.use(morgan('combined'));
app.use(express.static(path.join(__dirname, 'public')));

app.get('/', (req, res) => {
    logger.info('Home page accessed');
    res.sendFile(path.join(__dirname, 'public', 'index.html'));
});

app.listen(PORT, () => {
    logger.info(`Server running on port ${PORT}`);
});


#app.js(for public folder)
console.log("Frontend JS loaded");

#logger.js
const { createLogger, transports, format } = require('winston');

const logger = createLogger({
    level: 'info',
    format: format.combine(
        format.timestamp(),
        format.json()
    ),
    transports: [
        new transports.File({ filename: 'app.log' }),
        new transports.Console()
    ]
});

module.exports = logger;


#package.json
{
  "name": "simple-web-app",
  "version": "1.0.0",
  "description": "A simple Node.js web application with Winston logging",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "winston": "^3.10.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "author": "",
  "license": "MIT"
}


#index.html
<!DOCTYPE html>
<html>
<head>
    <title>Simple Web App</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Hello from Simple Web App</h1>
    <script src="app.js"></script>
</body>
</html>

#style.css
body {
    background-color: #f5f5f5;
    text-align: center;
    font-family: Arial;
}
